<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ZZRH - Edit User Name</title>
    <link rel="stylesheet" href="/css/output.css">
    <link href="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11/dist/sweetalert2.all.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
        body {
            font-family: 'Inter', sans-serif;
        }
        .glass-effect {
            backdrop-filter: blur(10px);
            background: rgba(255, 255, 255, 0.9);
        }
        .gradient-bg {
            background: linear-gradient(135deg, #1f2937 0%, #000000 100%);
        }
        .hover-lift {
            transition: all 0.3s ease;
        }
        .hover-lift:hover {
            transform: translateY(-2px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15);
        }
        .profile-avatar {
            background: linear-gradient(135deg, #6b7280 0%, #1f2937 100%);
        }
        .input-focus:focus {
            outline: none;
            border-color: #1f2937;
            box-shadow: 0 0 0 3px rgba(31, 41, 55, 0.1);
        }
    </style>
</head>
<body class="bg-linear-to-br from-gray-50 via-gray-100 to-gray-50 min-h-screen">
    <!-- Header -->
    <header class="py-4 px-6 md:py-6 md:px-16 flex justify-between items-center">
        <!-- Logo -->
        <div class="text-[60px] md:text-[125px] font-bloverly tracking-tight" style="font-family: bloverly;font-weight: lighter;">
            #ZZRH
        </div>

        <!-- Desktop Navigation -->
        <nav class="hidden md:flex space-x-8 text-xl font-clash">
            <a href="#" class="hover:underline">New Arrival</a>
            <a href="#" class="hover:underline">Men's collection</a>
            <a href="#" class="hover:underline">About Us</a>
        </nav>
        <div class="hidden md:flex items-center gap-4">
            <button class="w-12 h-12 rounded-full bg-gray-200 flex items-center justify-center hover:bg-gray-300">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                </svg>
            </button>
            <button class="w-12 h-12 rounded-full bg-gray-200 flex items-center justify-center hover:bg-gray-300">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z"></path>
                </svg>
            </button>
            <button class="w-12 h-12 rounded-full bg-gray-200 flex items-center justify-center hover:bg-gray-300">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"></path>
                </svg>
            </button>
            <div class="relative group">
                <button class="w-12 h-12 rounded-full bg-gray-200 flex items-center justify-center hover:bg-gray-300">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                    </svg>
                </button>
                <span class="absolute top-14 left-1/2 -translate-x-1/2 bg-black text-white text-xs px-2 py-1 rounded opacity-0 group-hover:opacity-100 transition whitespace-nowrap">
                    <%= isLogged.email %>
                </span>
            </div>
            <a href="/logout">
                <button class="hidden md:block border border-black px-6 py-2 rounded-md text-sm font-clash hover:bg-black hover:text-white transition">
                    Logout
                </button>
            </a>
        </div>

        <!-- Mobile Menu Button -->
        <button id="mobileMenuButton" class="md:hidden border border-black px-4 py-1 rounded text-sm font-clash">
            Menu
        </button>
    </header>

    <!-- Mobile Menu -->
    <div id="mobileMenu" class="hidden md:hidden px-6 pb-4 space-y-4 font-clash">
        <a href="#" class="block hover:underline">New Arrival</a>
        <a href="#" class="block hover:underline">Men's collection</a>
        <a href="#" class="block hover:underline">About Us</a>
        
        <div class="flex items-center gap-4">
            <button class="w-12 h-12 rounded-full bg-gray-200 flex items-center justify-center hover:bg-gray-300">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                </svg>
            </button>
            <button class="w-12 h-12 rounded-full bg-gray-200 flex items-center justify-center hover:bg-gray-300">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z"></path>
                </svg>
            </button>
            <button class="w-12 h-12 rounded-full bg-gray-200 flex items-center justify-center hover:bg-gray-300">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"></path>
                </svg>
            </button>
            <button class="w-12 h-12 rounded-full bg-gray-200 flex items-center justify-center hover:bg-gray-300">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                </svg>
            </button>
        </div>
    </div>

    <!-- Main Content -->
    <div class="max-w-7xl mx-auto px-6 py-8">
        <div class="flex gap-8">
            <!-- Sidebar -->
            <%- include('../Partials/user-sidebar') %>

            <!-- Edit Username Content -->
            <main class="flex-1">
                <!-- Breadcrumb -->
                <div class="mb-6">
                    <nav class="flex items-center gap-2 text-sm text-gray-600">
                        <a href="/profile" class="hover:text-gray-900 transition">Profile</a>
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                        </svg>
                        <span class="text-gray-900 font-medium">Edit User Name</span>
                    </nav>
                </div>

                <div class="glass-effect rounded-2xl shadow-xl border border-white/20 overflow-hidden">
                    <!-- Header -->
                    <div class="gradient-bg px-8 py-8 relative overflow-hidden">
                        <div class="absolute inset-0 opacity-10">
                            <div class="absolute top-0 right-0 w-64 h-64 bg-white rounded-full -translate-y-32 translate-x-32"></div>
                            <div class="absolute bottom-0 left-0 w-48 h-48 bg-white rounded-full translate-y-24 -translate-x-24"></div>
                        </div>
                        <div class="relative flex items-center gap-4">
                            <div class="w-16 h-16 bg-white/20 rounded-xl flex items-center justify-center">
                                <i class="fas fa-user-edit text-3xl text-white"></i>
                            </div>
                            <div>
                                <h2 class="text-3xl font-bold text-white mb-1">Edit Password</h2>
                                <p class="text-gray-300">Update your Password</p>
                            </div>
                        </div>
                    </div>

                    <!-- Form -->
                    <div class="px-8 py-8">
                        <% if(!otpVerified) {%>
                            <form  id="editPassword">
                                

                                <!-- New Username Input -->
                                <div class="mb-6">
                                    <label for="password" class="block text-sm font-semibold text-gray-700 mb-2">
                                        Enter Current password <span class="text-red-500">*</span>
                                    </label>
                                    <div class="relative">
                                        <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                                            <i class="fas fa-key text-gray-400"></i>
                                        </div>
                                        <input 
                                            type="text" 
                                            id="password" 
                                            name="password" 
                                            value=""
                                            class="input-focus w-full pl-12 pr-4 py-3 border-2 border-gray-300 rounded-xl text-gray-900 font-medium transition"
                                            placeholder="Enter your old password"
                                            required
                                            minlength="2"
                                            maxlength="50"
                                        >
                                    </div>
                                    <p class="mt-2 text-sm text-gray-500">
                                        <i class="fas fa-info-circle mr-1"></i>
                                        A 6-digit OTP will be sent to the registered mail address
                                    </p>
                                </div>

                                <!-- Validation Messages -->
                                <div id="validationMessage" class="hidden mb-6 p-4 rounded-xl"></div>

                                <!-- Action Buttons -->
                                <div class="flex gap-4 pt-4">
                                    <button 
                                        type="submit" 
                                        class="flex-1 gradient-bg text-white py-3 px-6 rounded-xl font-semibold hover-lift shadow-lg flex items-center justify-center gap-2"
                                    >
                                        <i class="fas fa-save"></i>
                                        Change
                                    </button>
                                    <a 
                                        href="/profile" 
                                        class="flex-1 bg-gray-200 text-gray-700 py-3 px-6 rounded-xl font-semibold hover:bg-gray-300 transition flex items-center justify-center gap-2"
                                    >
                                        <i class="fas fa-times"></i>
                                        Cancel
                                    </a>
                                </div>
                            </form>
                        <% } else{%>
                            <form  id="editNewPassword">
        
                                <div class="mb-6">
                                    <label for="password" class="block text-sm font-semibold text-gray-700 mb-2">
                                        Enter new password <span class="text-red-500">*</span>
                                    </label>
                                    <div class="relative mb-3">
                                        <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                                            <i class="fas fa-key text-green-400"></i>
                                        </div>
                                        <input 
                                            type="text" 
                                            id="newPassword" 
                                            name="password" 
                                            value=""
                                            class="input-focus w-full pl-12 pr-4 py-3 border-2 border-gray-300 rounded-xl text-gray-900 font-medium transition"
                                            placeholder="Enter your new password"
                                            required
                                            minlength="2"
                                            maxlength="50"
                                        >
                                        
                                    </div>
                                    <div class="relative ">
                                        <div class="absolute inset-y-0 left-0 pl-4 flex items-center pointer-events-none">
                                            <i class="fas fa-check text-green-400"></i>
                                        </div>
                                        <input 
                                            type="text" 
                                            id="Confirmpassword" 
                                            name="Confirmpassword" 
                                            value=""
                                            class="input-focus w-full pl-12 pr-4 py-3 border-2 border-gray-300 rounded-xl text-gray-900 font-medium transition"
                                            placeholder="Confirm your password"
                                            required
                                            minlength="2"
                                            maxlength="50"
                                        >
                                        
                                    </div>
                                    <p class="mt-2 text-sm text-green-500">
                                        <i class="fas fa-info-circle mr-1"></i>
                                        To keep your account secure, you’ll be logged out after changing your password
                                    </p>
                                </div>

                              
                                <div id="validationMessage" class="hidden mb-6 p-4 rounded-xl"></div>

                                <!-- Action Buttons -->
                                <div class="flex gap-4 pt-4">
                                    <button 
                                        type="submit" 
                                        class="flex-1 gradient-bg text-white py-3 px-6 rounded-xl font-semibold hover-lift shadow-lg flex items-center justify-center gap-2"
                                    >
                                        <i class="fas fa-save"></i>
                                        Change
                                    </button>
                                    <a 
                                        href="/profile" 
                                        class="flex-1 bg-gray-200 text-gray-700 py-3 px-6 rounded-xl font-semibold hover:bg-gray-300 transition flex items-center justify-center gap-2"
                                    >
                                        <i class="fas fa-times"></i>
                                        Cancel
                                    </a>
                                </div>
                            </form>
                        <% } %>
                    </div>
                </div>

                <!-- Help Card -->
                <!-- <div class="glass-effect rounded-2xl shadow-xl border border-white/20 p-6 mt-6">
                    <div class="flex items-start gap-4">
                        <div class="w-12 h-12 bg-blue-100 rounded-xl flex items-center justify-center flex-shrink-0">
                            <i class="fas fa-key text-blue-600 text-xl"></i>
                        </div>
                        <div>
                            <h3 class="text-lg font-bold mb-2 text-gray-800">Password Guidelines</h3>
                            <ul class="space-y-2 text-sm text-gray-600">
                                 <li class="flex items-start gap-2">
                                    <i class="fas fa-check text-green-500 mt-0.5"></i>
                                    <span>Choose a name that represents you</span>
                                </li> 
                                <li class="flex items-start gap-2">
                                    <i class="fas fa-check text-green-500 mt-0.5"></i>
                                    <span>Keep it minimum 6 characters</span>
                                </li>
                                <li class="flex items-start gap-2">
                                    <i class="fas fa-check text-green-500 mt-0.5"></i>
                                    <span>Use letters,number,symbols....</span>
                                </li>
                                <li class="flex items-start gap-2">
                                    <i class="fas fa-check text-green-500 mt-0.5"></i>
                                    <span>Avoid offensive or inappropriate language</span>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div> -->
            </main>
        </div>
    </div>

    <script>
        // Mobile menu toggle
        const menuButton = document.getElementById('mobileMenuButton');
        const mobileMenu = document.getElementById('mobileMenu');

        menuButton.addEventListener('click', () => {
            mobileMenu.classList.toggle('hidden');
        });

        // Character counter and progress bar
        const usernameInput = document.getElementById('password');
        const charCount = document.getElementById('charCount');
        const charProgress = document.getElementById('charProgress');
        const validationMessage = document.getElementById('validationMessage');

        function updateCharCounter() {
            const length = usernameInput.value.length;
            const maxLength = 6;
            const percentage = (length / maxLength) * 100;

            // charCount.textContent = `${length} / ${maxLength}`;
            // charProgress.style.width = `${percentage}%`;

            // // Change color based on length
            // if (length === 0) {
            //     charProgress.className = 'h-full bg-gray-300 transition-all duration-300';
            // } else if (length < 6) {
            //     charProgress.className = 'h-full bg-red-500 transition-all duration-300';
            // } else if (length > 40) {
            //     charProgress.className = 'h-full bg-yellow-500 transition-all duration-300';
            // } else {
            //     charProgress.className = 'h-full bg-gradient-to-r from-gray-700 to-gray-900 transition-all duration-300';
            // }
        }

        //usernameInput.addEventListener('input', updateCharCounter);

        // Form validation
        const verificationForm = document.getElementById('editPassword');
        const newPasswordForm = document.getElementById('editNewPassword')
        if(verificationForm){
            verificationForm.addEventListener('submit', async (e) => {
                e.preventDefault()
                const password = usernameInput.value.trim();

                validationMessage.classList.add('hidden');

                showValidationMessage('Updating password ...', 'success');

                try{
                            
                    const request = await fetch('/profile/change-password',{
                        method:"POST",
                        headers:{
                            "Content-Type":"application/json"
                        },
                        body:JSON.stringify({password})
                    })

                    const result = await request.json()

                    if(!result.success){
                        return Swal.fire({
                            toast: true,
                            icon: 'error',
                            title: result.message,
                            position: 'top-right',
                            showConfirmButton: false,
                            timer: 3000
                        });
                    }
                    window.location.href = result.redirect
                    
                }catch(e){
                    console.log("Front-end-error :\n",e)
                }

            });
        }

        if(newPasswordForm){

            newPasswordForm.addEventListener('submit', async (e) => {
                e.preventDefault()

                validationMessage.classList.add('hidden');

                const password = document.getElementById('newPassword').value
                const confirm_password = document.getElementById('Confirmpassword').value

                if(/(.)\1{2,}/.test(password)){
                    e.preventDefault()
                    showValidationMessage('Same characters cannot be repeted more than 2 times','error')
                    return;
                }
                if(password.length<6){
                    e.preventDefault()
                    showValidationMessage('Minimum 6 characters','error')
                    return;
                }
                if(password === ''){
                    e.preventDefault()
                    showValidationMessage('Enter a password','error')
                    return;
                }
                if(password !== confirm_password){
                    e.preventDefault()
                    showValidationMessage('Password doesn\'t match','error')
                    return;
                }

                

                showValidationMessage('Checking password ...', 'success');

                try{
                            
                    const request = await fetch('/profile/change-password',{
                        method:"POST",
                        headers:{
                            "Content-Type":"application/json"
                        },
                        body:JSON.stringify({password})
                    })

                    const result = await request.json()

                    if(!result.success){
                        return Swal.fire({
                            toast: true,
                            icon: 'error',
                            title: result.message,
                            position: 'top-right',
                            showConfirmButton: false,
                            timer: 3000
                        });
                    }
                    
                     window.location.href = result.redirect
                    
                }catch(e){
                    console.log("Front-end-error :\n",e)
                }

            });

        }
        function showValidationMessage(message, type) {
            validationMessage.classList.remove('hidden');
            
            if (type === 'error') {
                validationMessage.className = 'mb-6 p-4 rounded-xl bg-red-50 border border-red-200 text-red-700 flex items-center gap-2';
                validationMessage.innerHTML = `<i class="fas fa-exclamation-circle"></i><span>${message}</span>`;
            } else if (type === 'warning') {
                validationMessage.className = 'mb-6 p-4 rounded-xl bg-yellow-50 border border-yellow-200 text-yellow-700 flex items-center gap-2';
                validationMessage.innerHTML = `<i class="fas fa-exclamation-triangle"></i><span>${message}</span>`;
            } else if (type === 'success') {
                validationMessage.className = 'mb-6 p-4 rounded-xl bg-green-50 border border-green-200 text-green-700 flex items-center gap-2';
                validationMessage.innerHTML = `<i class="fas fa-check-circle"></i><span>${message}</span>`;
            }
        }

        
        //updateCharCounter();
    </script>
</body>
</html>